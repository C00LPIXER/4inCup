rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Player photos - anyone can upload and read
    match /players/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
